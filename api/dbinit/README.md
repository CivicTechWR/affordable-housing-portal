# dbinit

Database bootstrap SQL used to initialize the application's Postgres database.

This fork does not use the Docker-based dbinit wrapper from upstream.

`rds.init.sql` remains as the reusable SQL bootstrap script for environments that provision database roles outside local development.
